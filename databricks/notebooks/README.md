# Databricks Silver Transformation

This folder contains the Azure Databricks notebook used to transform NYC Taxi data from the Bronze layer into the Silver layer.

The notebook reads raw Parquet data from ADLS Gen2, performs data cleansing and transformation, and writes the processed data to the Silver layer.
